name: Functional Tests

on:
  pull_request:
    branches: [main]
    paths:
      - "scripts/**"
      - "action.yml"
      - ".github/workflows/functional_tests.yml"
      - ".github/functional_test_toolchain"
      - ".github/functional_tests/**"
  workflow_dispatch:
    inputs:
      toolchain:
        description: "The Lean toolchain to use when running the tests. Defaults to the toolchain pinned in .github/functional_test_toolchain."
        required: false
        default: ""
  # Allows other workflows (e.g. release.yml) to run the functional tests.
  workflow_call:
    inputs:
      toolchain:
        description: "The Lean toolchain to use when running the tests. Defaults to the toolchain pinned in .github/functional_test_toolchain."
        required: false
        type: string
        default: ""

env:
  # Toolchain without the bundled `leanchecker` binary,
  # used to cover the external `lean4checker` fallback path
  legacy_toolchain: leanprover/lean4:v4.26.0
  # `lake check --paranoid` landed in v4.35.0-rc2, which is newer than the toolchain pinned in
  # `.github/functional_test_toolchain`, so the `lake-check` test pins its own.
  lake_check_toolchain: leanprover/lean4:v4.35.0-rc2

jobs:
  # The default toolchain is pinned in `.github/functional_test_toolchain`
  # rather than in this file so that the `Update Functional Test Toolchain`
  # workflow can bump it without a token holding the `workflows` permission,
  # which the default `GITHUB_TOKEN` cannot be granted.
  resolve-toolchain:
    runs-on: ubuntu-latest
    outputs:
      toolchain: ${{ steps.resolve.outputs.toolchain }}
    steps:
      - uses: actions/checkout@v7
      - name: Resolve the functional test toolchain
        id: resolve
        env:
          # The toolchain input is unset when the workflow is triggered
          # by a pull request, in which case the pinned toolchain is used.
          input_toolchain: ${{ inputs.toolchain }}
        run: |
          toolchain="${input_toolchain:-$(tr -d '[:space:]' < .github/functional_test_toolchain)}"
          if [ -z "$toolchain" ]; then
            echo "::error::failed to resolve the functional test toolchain"
            exit 1
          fi
          echo "Using toolchain: $toolchain"
          echo "toolchain=$toolchain" >> "$GITHUB_OUTPUT"

  lake-init-success:
    needs: resolve-toolchain
    runs-on: ubuntu-latest
    strategy:
      matrix:
        # run `lean-action` on a package generated with `lake init` for:
        #  - a standalone package
        #  - a package with a mathlib dependency
        #  - a package with a `lakefile.toml` file
        # see ./github/functional_tests/lake_init/action.yml for more details on lake-init-arguments
        lake-init-arguments: ["standalone", "mathdep math", "tomltest .toml"]
    steps:
      - uses: actions/checkout@v7
      - uses: ./.github/functional_tests/lake_init_success
        with:
          lake-init-arguments: ${{ matrix.lake-init-arguments}}
          toolchain: ${{ needs.resolve-toolchain.outputs.toolchain }}

  lake-init-failure:
    needs: resolve-toolchain
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: ./.github/functional_tests/lake_init_failure
        with:
          toolchain: ${{ needs.resolve-toolchain.outputs.toolchain }}

  auto-config-true:
    needs: resolve-toolchain
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: ./.github/functional_tests/auto_config_true
        with:
          toolchain: ${{ needs.resolve-toolchain.outputs.toolchain }}

  auto-config-false:
    needs: resolve-toolchain
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: ./.github/functional_tests/auto_config_false
        with:
          toolchain: ${{ needs.resolve-toolchain.outputs.toolchain }}

  auto-config-false-legacy-leanchecker:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: ./.github/functional_tests/auto_config_false
        with:
          toolchain: ${{ env.legacy_toolchain }}

  lake-build-args:
    needs: resolve-toolchain
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: ./.github/functional_tests/lake_build_args
        with:
          toolchain: ${{ needs.resolve-toolchain.outputs.toolchain }}

  lake-test-args:
    needs: resolve-toolchain
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: ./.github/functional_tests/lake_test_args
        with:
          toolchain: ${{ needs.resolve-toolchain.outputs.toolchain }}

  detect-mathlib:
    needs: resolve-toolchain
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: ./.github/functional_tests/mathlib_dependency
        with:
          toolchain: ${{ needs.resolve-toolchain.outputs.toolchain }}

  lake-test-success:
    needs: resolve-toolchain
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: ./.github/functional_tests/lake_test_success
        with:
          toolchain: ${{ needs.resolve-toolchain.outputs.toolchain }}

  lake-test-failure:
    needs: resolve-toolchain
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: ./.github/functional_tests/lake_test_failure
        with:
          toolchain: ${{ needs.resolve-toolchain.outputs.toolchain }}

  lake-lint-success:
    needs: resolve-toolchain
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: ./.github/functional_tests/lake_lint_success
        with:
          toolchain: ${{ needs.resolve-toolchain.outputs.toolchain }}

  lake-lint-failure:
    needs: resolve-toolchain
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: ./.github/functional_tests/lake_lint_failure
        with:
          toolchain: ${{ needs.resolve-toolchain.outputs.toolchain }}

  lake-lint-args:
    needs: resolve-toolchain
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: ./.github/functional_tests/lake_lint_args
        with:
          toolchain: ${{ needs.resolve-toolchain.outputs.toolchain }}

  lake-check-test-failure:
    needs: resolve-toolchain
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: ./.github/functional_tests/lake_check_test_failure
        with:
          toolchain: ${{ needs.resolve-toolchain.outputs.toolchain }}

  subdirectory-lake-package:
    needs: resolve-toolchain
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: ./.github/functional_tests/subdirectory_lake_package
        with:
          toolchain: ${{ needs.resolve-toolchain.outputs.toolchain }}

  subdirectory-lake-package-lean-checker:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: ./.github/functional_tests/subdirectory_lake_package_lean_checker
        with:
          toolchain: ${{ env.legacy_toolchain }}

  reinstall-transient-toolchain:
    needs: resolve-toolchain
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: ./.github/functional_tests/reinstall-transient-toolchain
        with:
          toolchain: ${{ needs.resolve-toolchain.outputs.toolchain }}

  mk_all-check:
    needs: resolve-toolchain
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: ./.github/functional_tests/mk_all-check
        with:
          toolchain: ${{ needs.resolve-toolchain.outputs.toolchain }}

  mk_all-args:
    needs: resolve-toolchain
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: ./.github/functional_tests/mk_all_args
        with:
          toolchain: ${{ needs.resolve-toolchain.outputs.toolchain }}

  axiom_audit:
    needs: resolve-toolchain
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: ./.github/functional_tests/axiom_audit
        with:
          # axiom-audit builds with the project's toolchain, so use a recent one.
          toolchain: ${{ needs.resolve-toolchain.outputs.toolchain }}

  # `lake check` sandboxes with bubblewrap, which GitHub-hosted runners do not permit without a
  # privileged change to the runner. Namespace serves a privileged container, which permits it
  # outright, so that is where this runs. The `-with-features` suffix is what makes the feature
  # label apply. These labels are per-organisation configuration, and a job whose labels an
  # organisation does not serve sits queued rather than failing, so keep this to the repository
  # known to be provisioned for them.
  lake-check-namespace:
    if: github.repository == 'leanprover/lean-action'
    runs-on:
      - nscloud-ubuntu-24.04-amd64-2x4-with-features
      - namespace-features:container.privileged=true
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v7
      - uses: ./.github/functional_tests/lake_check
        with:
          toolchain: ${{ env.lake_check_toolchain }}

  lake-check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: ./.github/functional_tests/lake_check
        with:
          toolchain: ${{ env.lake_check_toolchain }}
          # GitHub-hosted runners restrict the user namespaces bubblewrap needs.
          sandbox: apparmor

  # Pinned to a toolchain that predates `lake check`, rather than to the moving default: once the
  # default supports it, this would otherwise quietly stop testing feature detection.
  lake-check-unsupported-toolchain:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: ./.github/functional_tests/lake_check_unsupported
        with:
          toolchain: ${{ env.legacy_toolchain }}

  macos-runner:
    needs: resolve-toolchain
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v7
      - uses: ./.github/functional_tests/lake_init_success
        with:
          lake-init-arguments: "standalone"
          toolchain: ${{ needs.resolve-toolchain.outputs.toolchain }}

  windows-runner:
    needs: resolve-toolchain
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v7
      - uses: ./.github/functional_tests/lake_init_success
        with:
          lake-init-arguments: "standalone"
          toolchain: ${{ needs.resolve-toolchain.outputs.toolchain }}
